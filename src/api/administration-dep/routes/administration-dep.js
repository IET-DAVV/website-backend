'use strict';

/**
 * administration-dep router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::administration-dep.administration-dep');
