'use strict';

/**
 * administration-dep service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::administration-dep.administration-dep');
