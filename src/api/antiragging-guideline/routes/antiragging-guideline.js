'use strict';

/**
 * antiragging-guideline router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::antiragging-guideline.antiragging-guideline');
