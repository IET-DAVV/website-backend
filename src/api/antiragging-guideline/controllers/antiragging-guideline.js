'use strict';

/**
 * antiragging-guideline controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::antiragging-guideline.antiragging-guideline');
