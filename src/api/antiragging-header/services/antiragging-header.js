'use strict';

/**
 * antiragging-header service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::antiragging-header.antiragging-header');
