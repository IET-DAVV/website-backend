'use strict';

/**
 * antiragging-header router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::antiragging-header.antiragging-header');
